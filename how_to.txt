[ACTIVATE WEBSITE ON PC]
python -m uvicorn main:app --reload

[GIT PUSH]
cd project-path
git add .
git commit -m "Describe what you changed"
git push origin main

[NGINX CONFIGURATION]
sudo apt-get update
sudo apt-get install -y python3-pip python3-venv jinja2 nginx
git clone https://github.com/FakeEstateDeveloper/project-name.git
cd project-name
python3 -m venv venv
source venv/bin/activate
pip3 install -r requirements.txt (contains fastapi & uvicorn & jninja2)
sudo vim /etc/nginx/sites-available/app-name
server {
    listen 80;
    server_name ec2_public_ip;

    location / {
        proxy_pass http://127.0.0.1:8000;
    }
}
sudo service nginx restart

[ACTIVATE WEBSITE ON EC2 INSTANCE]
source venv/bin/activate
uvicorn main:app --host 0.0.0.0 --port 8000